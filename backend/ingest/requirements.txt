# Upper bounds pin the majors so a breaking release can't ship itself through
# the push-to-deploy pipeline unreviewed.
Flask>=3.0,<4
gunicorn>=21.2,<24
google-genai>=1.0,<2
google-api-python-client>=2.100,<3
google-auth>=2.28,<3
# Both arrive as transitive deps of google-api-python-client, but main.py imports
# them BY NAME to build the Sheets client with its own socket timeout (see
# `_timed_http`) — so they are declared here rather than relied on by accident.
google-auth-httplib2>=0.2,<1
httplib2>=0.22,<1
google-cloud-tasks>=2.16,<3
# Durable storage for the coach's generated cards, taxonomy, memory and threads
# (see ingest/coach_store.py — /tmp on Cloud Run doesn't survive a cold start).
google-cloud-storage>=2.14,<4
