[Unit]
Description=Health Tracker coach weekly review (replaces Cloud Scheduler coach-weekly)

[Timer]
# Was 0 10 * * MON Europe/Lisbon.
OnCalendar=Mon *-*-* 10:00:00
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
